"use client"

const { createSlice } = require("@reduxjs/toolkit")

const addressesSlice = createSlice({
  name: "addresses",
  initialState: [],
  reducers: {

  }
})

export const {} = addressesSlice;

export default addressesSlice.reducer;